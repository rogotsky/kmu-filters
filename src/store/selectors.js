export const filters = (state) => state.filters;
export const currentPage = (state) => state.currentPage;
export const currentQuery = (state) => state.currentQuery;
export const searchValue = (state) => state.searchValue;
